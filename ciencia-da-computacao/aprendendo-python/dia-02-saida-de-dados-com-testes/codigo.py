# retorna true se o numero e impares, se nao false
def is_odd(number):
    return number % 2 != 0

# retorna a divisao de um numero por outro


def division(number, divisor):
    return number / divisor
